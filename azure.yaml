# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aoai-realtime-token
metadata:
  template: functions-quickstart-python-azd@1.0.0
services:
  api:
    project: ./api
    language: python
    host: function
